# The `files` whitelist in package.json governs what ships; this is belt-and-braces.
# Never publish the downloaded binary or archives — they're fetched per-platform on install.
bin/mercadona
bin/mercadona.exe
*.tar.gz
*.zip
node_modules/
